<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <!-- Profile Image -->
        <ImageView
            android:id="@+id/editProfileImage"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/pogi"
            android:scaleType="centerCrop"
            android:layout_marginBottom="16dp" />

        <!-- Name -->
        <EditText
            android:id="@+id/editName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter Name"
            android:layout_marginTop="8dp" />

        <!-- Sayings -->
        <EditText
            android:id="@+id/editSayings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter Sayings"
            android:layout_marginTop="8dp" />

        <!-- Interests -->
        <EditText
            android:id="@+id/editInterests"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter Interests"
            android:layout_marginTop="8dp" />

        <!-- Featured Photos -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap to change featured photos"
            android:textStyle="bold"
            android:layout_marginTop="24dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="8dp">

            <ImageView
                android:id="@+id/editFeatured1"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/placeholder"
                android:layout_margin="4dp"
                android:scaleType="centerCrop" />

            <ImageView
                android:id="@+id/editFeatured2"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/placeholder"
                android:layout_margin="4dp"
                android:scaleType="centerCrop" />

            <ImageView
                android:id="@+id/editFeatured3"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/placeholder"
                android:layout_margin="4dp"
                android:scaleType="centerCrop" />
        </LinearLayout>

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSaveProfile"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Save Profile"
            android:layout_marginTop="24dp" />

    </LinearLayout>
</ScrollView>
